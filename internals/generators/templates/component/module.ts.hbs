import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { NgModule } from '@angular/core';
import { {{properCase name}}Component } from './{{dashCase name}}.component';
{{#if odd}}

// ngrx
import { StoreModule } from '@ngrx/store';
import { EffectsModule } from '@ngrx/effects';
import { reducers } from '../ngrx/{{dashCase plural}}/reducers';
import { {{properCase odd}}Effects } from '../ngrx/{{dashCase plural}}/effects/{{dashCase odd}}';
import { {{properCase odd}}Service } from '../ngrx/{{dashCase plural}}/services/{{dashCase odd}}';
{{/if}}

@NgModule({
  declarations: [
    {{properCase name}}Component,
  ],
  imports: [
    CommonModule,
    FormsModule,
    {{#if odd}}
    StoreModule.forFeature('{{camelCase plural}}', reducers),
    EffectsModule.forFeature([{{properCase odd}}Effects]),
    {{/if}}
  ],
  providers: [
    {{#if odd}}
    {{properCase odd}}Service,
    {{/if}}
  ]
})
export class {{properCase name}}Module {
}
